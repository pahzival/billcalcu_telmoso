<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Create Account - Electric Bill Calculator</title>

    <style>
        /* General Style */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Arial', sans-serif;
            background-color: #f0f0f0;
        }

        .container {
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            padding: 20px;
        }

        .form-container {
            background-color: white;
            padding: 2rem;
            border-radius: 8px;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
            width: 100%;
            max-width: 400px;
        }

        header {
            text-align: center;
            margin-bottom: 1rem;
        }

        .logo {
            width: 50px;
            height: 50px;
            margin-bottom: 1rem;
        }

        h1 {
            font-size: 24px;
            margin-bottom: 0.5rem;
        }

        header p {
            font-size: 14px;
            margin-bottom: 1.5rem;
            color: #555;
        }

        form {
            display: flex;
            flex-direction: column;
        }

        input {
            margin-bottom: 15px;
            padding: 10px;
            font-size: 16px;
            border: 1px solid #dcdcdc;
            border-radius: 5px;
        }

        button {
            background-color: #5ed6b2;
            padding: 12px;
            color: white;
            font-size: 16px;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            margin-top: 5px;
        }

        button:hover {
            background-color: #4abba5;
        }

        footer {
            text-align: center;
            margin-top: 1rem;
        }

        footer a {
            color: #5ed6b2;
            text-decoration: none;
        }

        footer a:hover {
            text-decoration: underline;
        }

        /* Responsive */
        @media (max-width: 480px) {
            .form-container {
                padding: 1.5rem;
            }

            h1 {
                font-size: 20px;
            }

            input, button {
                font-size: 14px;
            }
        }
    </style>

</head>
<body>

    <div class="container">
        <div class="form-container">
            <header>
                <img src="logo.png" alt="Logo" class="logo">
                <h1>Create Account</h1>
                <p>Join Electric Bill Calculator to start tracking your electricity usage</p>
            </header>

            <form action="#" method="POST">
                <input type="text" id="full-name" name="full-name" placeholder="Full Name" required>
                <input type="email" id="email" name="email" placeholder="Email Address" required>
                <input type="text" id="address" name="address" placeholder="Address" required>
                <input type="tel" id="phone-number" name="phone-number" placeholder="Phone Number" required>
                <input type="password" id="password" name="password" placeholder="Password" required>
                <input type="password" id="confirm-password" name="confirm-password" placeholder="Confirm Password" required>

                <button type="submit" class="btn-create">Create Account</button>
            </form>

            <footer>
                <p>Already have an account? <a href="login.html">Log In</a></p>
            </footer>
        </div>
    </div>

</body>
</html>
